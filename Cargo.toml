[package]
name = "whalemail"
version = "0.1.0"
edition = "2024"

[dependencies]
strum = { version = "0.27.2", features = ["derive"] }
tokio = { version = "1.45.1", features = ["rt", "macros", "net"] }
